using namespace std;
#include <bits/stdc++.h>
int main()
{
   int n; cin>> n;
   // vector - pair
   pair <int, int> a[n];
   for (int i = 0; i < n; i++)
   {
      cin>> a[i].first >> a[i].second;
   }
   vector <pair<int, int>> v;
   pair <int , pair <int, int>> p;
   p.first = 10;
   p.second;
   // set
   set <int> s;
}